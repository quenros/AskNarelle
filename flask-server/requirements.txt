# Core Framework

Flask
flask-cors
gunicorn
python-dotenv
requests~=2.32.3
schedule~=1.2.2
pytz

# Database

pymongo~=4.11.1
dnspython

# Azure

azure-storage-blob
azure-search-documents
azure-identity~=1.20.0

# AI & LangChain

openai
langchain
langchain-community
langchain-openai~=0.3.6
langchain-text-splitters~=0.3.6

# Document Processing (Lightweight alternatives to unstructured)

python-docx
python-pptx
PyMuPDF

# Utilities & Security

pydantic>=2.9.2,<3
bcrypt~=4.3.0
cryptography>=42
cffi

# --- EXCLUDED TO FIX DEPLOYMENT SIZE ---

# The following were removed because they exceed Azure App Service disk limits:

# - torch (PyTorch)

# - transformers / sentence-transformers (HuggingFace models)

# - unstructured / unstructured[xlsx] (Massive file parsing suite)

# - chromadb (Vector DB - you are using Azure AI Search instead)

# - ragas / datasets (Evaluation tools - run these locally, not in prod)

# - ffmpeg-python / ffmpegcv (Video processing - you use Azure Video Indexer)

# - fastapi / uvicorn (Your app.py uses Flask)